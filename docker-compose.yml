version: '3'
services:
  server:
    restart: always
    env_file: .env
    build:
      context: .
      dockerfile: DockerfileServer
    ports:
      - "5000:5000"

  client:
    restart: always
    build:
      context: .
      dockerfile: DockerfileClient
    ports:
      - "8080:8080"


